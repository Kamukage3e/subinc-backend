# Billing Management Migrations

All schema migrations for billing-management are in this folder. Each migration is production-grade, type-safe, and supports multi-tenancy.

## Tables
- invoices
- invoice_items
- billing_feature_flags

All migrations are reviewed for SaaS readiness before merge. No placeholders, no bloat, no non-prod content. 