package tax

type TaxServiceAdapter struct {
	Store *PostgresStore
}

// Add methods as needed, e.g.:
// func (a *TaxServiceAdapter) CreateTax(t Tax) (Tax, error) {
// 	return a.Store.CreateTax(context.Background(), t)
// }
// ...
